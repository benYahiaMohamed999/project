// ignore_for_file: file_names

import 'package:flutter/material.dart';

class ShowCategorieScreen extends StatelessWidget {
  const ShowCategorieScreen({super.key, required this.id});

  final int id;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
